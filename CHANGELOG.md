# 更新日志

## 2.2.0 - 2026-06-20

### 新增

- 新增 AstrBot 插件 Dashboard 完整配置面板，可在 WebUI 中查看、编辑并保存 `_conf_schema.json` 中的全部设置项。
- 新增 `GET /astrbot_plugin_period/config` 与 `POST /astrbot_plugin_period/config`，支持 schema、默认值、provider 下拉选项和运行态即时生效。
- WebUI 新增分组设置页：周期基础、注入与指令、提示词显示、生效范围、情绪系统、提示词压缩。
- WebUI 情绪系统分组增加实验性功能警告，并在开启情绪系统时进行二次确认。
- WebUI 下拉框、标签编辑器、数字输入框、会话列表和总览卡片完成视觉升级，统一为黑白留白卡片风格。

### 调整

- 默认动态提示词注入位置改为 `extra_user_content_parts`，对齐 AstrBot 临时内容注入方式，减少对用户原始消息和对话历史的污染。
- 动态提示词最大字数默认值改为 `0`，表示默认不截断；仍可在配置中设置正整数限制长度。
- 更新内置身体感知提示词与四阶段默认文案，使提示更强调保持原人格、避免主动暴露阶段名、减少过度表现。
- WebUI 保存配置时会保留未知旧字段和条件隐藏字段，避免误删未来配置或用户已有隐藏配置。

### 修复

- 修复 WebUI 保存后配置未立即同步到插件运行态的问题。
- 修复 provider 选择、嵌套对象、列表字段、长文本字段在 WebUI 保存时的校验和合并问题。
- 修复默认注入位置相关测试，使未显式配置时走 `extra_user_content_parts`。

### 兼容性

- 仍要求 AstrBot `>=4.24`。
- 已存在的用户配置不会被 WebUI 保存流程主动删除；新增默认值只会在字段缺失时作为兜底使用。
