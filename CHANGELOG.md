# 更新日志

## 3.0.0 - 2026-08-14

### 新增

- 情绪系统 vNext：状态升级为自然语言内在心境（当前心境/最近原因/是否好转/是否完全恢复/恢复原因/当前动作），默认以临时内容注入，不写入对话历史。
- 调用②（主模型决策）改为参考 AstrBot 装配的真实对话历史，条数可配置（0~100，默认 30）。
- 一轮可组合多个软动作；动作组合严格校验（冷暴力/已读不回互斥、硬软不共存、冲突整组拒绝）。
- 冷暴力新增触发方式选择：`immediate` 当轮沉默 / `after_expression` 本轮自然表达最后一句后下轮起沉默。
- 新增情绪日记（实验性）：专用小模型后台异步维护按人隔离的自然语言长期记忆，超限必须删除最早条目并反复确认；默认注入正式回复与情绪决策。
- 新增可选的跨人只读检索工具 `period_diary_lookup`（默认关闭）：按 QQ 号或唯一昵称查看同一机器人名下他人日记，结果临时注入不落历史。
- 新增管理员指令 `/period diary <QQ号>`、`/period diaryclear <QQ号>`。

### 调整

- 情绪系统明确为生理周期的附属功能：只在周期总开关开启、UMO 范围匹配、会话启用且周期可计算时运行；预热轮数/注入频率/关键词只控制身体提示展示，不再阻断情绪判断。
- 动作决策本轮决定本轮生效，不再延后一轮。
- 冷暴力/已读不回的沉默移至请求侧：被拦截轮次不再调用正式模型，也不会产生写入历史的隐藏回复；用户消息仍会写入对话历史。
- 已读不回轮数语义明确：`rounds=3` 表示含触发当轮共拦截 3 条消息。
- 彻底移除固定预设台词，所有情绪表现由主模型按人格自然表达；删除 `cold_violence_behavior` 配置（旧值保留但忽略）。
- `mood_detector_read_system_prompt` 现在只控制调用①是否读取截断人格摘要；调用②始终使用完整人格。
- 移除 `fake_tool_call` 注入位置，仍配置它的旧用户自动安全降级为 `extra_user_content_parts` 并记录一次诊断。
- `/period lift` 成为不可关闭的安全出口：不受情绪开关与指令权限限制，`global` 模式下同样可用；`/period mood` 在开关关闭时也可查看；`/period moodreset` 只删情绪不删日记。
- 模型调用失败采用保守策略：保持原状态到原到期时间/原剩余轮数，不延长、不激活新动作。
- 隐私收紧：不再持久化用户消息原文，不再记录完整人格与主模型决策回答；状态历史与诊断只保留脱敏摘要。

### 修复

- 修复情绪系统被周期注入门禁（warmup/inject_mode/关键词）误阻断的问题。
- 修复响应侧"先生成隐藏回复再清空"导致用户看不到的助手回复写入 AstrBot 历史的问题。
- 修复 `system_prompt_append` 位置注入的情绪状态/日记被身体锚点重建覆盖的问题；锚点现在基于当前 system_prompt 追加。
- 移除 `user_message_before` 注入位置（它会改写用户消息并写入对话历史），仍配置时自动降级为 `extra_user_content_parts`。
- 修复已有硬动作时跳过调用①的问题：三段架构不再跳过，筛选为否也强制进入②③。
- 修复第三方 Agent Runner 误判：现在以 `req.conversation` 是否存在为唯一判据（全局 Context 恒有 conversation_manager，不能作为内部 Agent 证据）。
- 调用②改为绑定本轮实际 Provider（`selected_provider`/会话偏好），当前消息以 `req.prompt` 为准，避免与正式回复模型不一致。
- 第三阶段校验收紧：布尔字段必须是真 bool、参数类型错误整组拒绝（不再静默改默认值）、冷暴力必须带沉默方式、矛盾恢复状态整份作废；校验器保证不抛异常穿透请求钩子。
- 修复模型重复激活同名硬动作导致已读不回/冷暴力无限"续杯"的问题：同名旧动作在生效时拒绝续期，保持原到期时间/剩余轮数。
- 修复 `/period lift` 只清硬动作不退情绪的问题：无硬动作时也会把当前内在情绪标记为手动恢复。
- 修复日记队列：失败事件延迟重试并阻塞同键后续事件（FIFO 不再被超车）；`diaryclear` 连同未处理事件与在处理草稿一起清除，已清除日记不再被后台任务复活。
- 修复时间比较：ISO 时间统一解析为 UTC 后比较，非法/无时区时间在迁移与载入时丢弃，损坏的 v3 双硬动作全部清除并记录诊断。
- 日记事件现在携带最近原因、是否好转与日记主人昵称/QQ 号；修复当前事件条目被工具删除后仍可能确认提交的边缘情况。
- 隐私加固：JSON 解析失败不再记录模型原始输出；心境/原因/摘要字段在提交前对当前消息与本轮历史做用户原话包含检测，命中即清空；调用①的筛选理由不再写入日志。
- 完全恢复提交当轮不再提前扣减恢复保留条数（从下一条有效消息起计 10 条）；空白 stable 状态也注入极简平稳占位，保证每个有效请求都告知模型当前情绪状态。
- 修复调用①失败被当作"筛选为否"的问题：失败与否定严格区分，已有硬动作时①失败直接按原规则沉默，不再给②③解除旧动作的机会。
- 修复日记事件入队/落盘异常穿透主请求链的问题：故障只记诊断，正常回复不受影响。
- 修复 diaryclear 与正在提交的后台任务之间的竞态：提交与清除共用临界区，清除后不再复活。
- 日记队列改为严格 FIFO：失败/暂停事件按指数退避（默认 60s 起、封顶 300s）持续重试，后续事件不再超车；确认不再需要的事件用 `diaryclear` 清除。
- 修复情绪/日记开关关闭后重启恢复 outbox 仍调用模型的问题：关闭时事件延后处理，重开后自动恢复。
- 第三阶段校验进一步收紧：`mood_update` 及其文本字段为必填且禁止类型强转；完全恢复同时新激活硬动作视为自相矛盾整组拒绝。
- 修复 diaryclear 在"失败重试窗口"仍可能复活日记的问题：提交时除纪元比对外，还在同一临界区校验事件仍在 outbox（重试会重新捕获纪元，单靠纪元防不住）。
- 修复 outbox 落盘失败被当作成功入队的问题：写入失败回滚缓存、不触发后台任务、明确记日志，不再重启后悄悄丢事件。
- 修复周期总开关（auto_inject）关闭后日记后台任务仍调用模型的问题：日记门控现在包含周期总开关。
- 修复调用①返回坏 JSON/空响应被当作"无需介入"的问题：不可解析一律按失败处理，已有硬动作时保守沉默。
- 修复硬沉默在情绪状态落盘失败时被异常打断、导致正式回复意外发出的问题：落盘失败只记诊断，沉默照常生效。
- 第三阶段顶层字段 `actions`/`lift_actions`/`silence_mode`/`reasoning_summary` 缺失即整份作废，不再按默认值接受不完整输出。
- 情绪与日记的 LLM 调用失败日志只记异常类型，不再记录异常消息（自定义 Provider 可能在异常中回显含人格/历史/日记的请求体）。
- 修复跨人日记检索结果截断后追加后缀导致总长超过配置上限的问题。
- 修复日记存储在落盘失败时内存缓存仍被更新的问题：缓存现在严格镜像磁盘，写入失败时未落盘日记不会被当前进程读取注入，`diaryclear` 在落盘失败时如实回答"清除未生效"，不再假成功后重启复活。
- 修复调用①把字符串 `"false"` 或缺失的 `need_intervention` 字段当作正常筛选结果的问题：非真布尔一律按调用失败处理。
- 修复处理中的日记事件在开关关闭后仍继续调用模型并提交的问题：工具循环每一步与提交落盘前都会复查开关，关闭即停、事件延后。
- 修复日记失败退避在极端长循环（约 3.5 天持续失败）下浮点溢出导致事件出队丢失的问题。
- 第三阶段校验再收紧：显式 `params: null` 整组拒绝（仅键缺失才用默认值）；`lift_actions` 重复项或由软动作充当解除目标均整份作废，不再静默去重。
- `/period reset` 现在会丢弃该会话来源的待处理日记事件（按来源 UMO 匹配，已提交日记保留）；日记处理总超时改为硬上限，单步只给剩余时间。
- 修复 `/period reset` 丢弃待处理事件与后台提交之间的竞态：丢弃现在按受影响用户在提交锁内执行，与"检查事件仍在 outbox + 提交落盘"的临界区互斥，提交窗口里的 reset 不会再让旧事件写入日记。
- 修复 `diaryclear` 落盘失败时仍抽干内存队列的问题：现在先删除、成功后才失效纪元并清理队列，失败时队列与 outbox 原样保留，FIFO 不受影响。
- 修复 MoodStore 与 DiaryStore 同款事务问题：情绪状态写盘失败不再污染内存缓存（缓存镜像磁盘），也不再向请求钩子与 `/period lift` 抛出 OSError——安全出口在磁盘故障下照常可用。
- 修复调用①收到非字符串畸形响应（如 dict）时 `AttributeError` 穿透请求钩子的问题：一律按调用失败走保守分支。
- 日记事件入队失败的日志与诊断只记异常类型，不再记录异常正文（自定义 Provider/存储异常可能回显请求信息）。
- 会话级周期失效（`/period toggle` 关闭、Dashboard 删除会话、白名单变更等）现在也会暂停该会话来源滞留日记事件的处理，周期重新有效后自动恢复；`/period reset` 清理失败时回复会如实提示。
- 修复 `/period lift` 在情绪状态落盘失败时仍回复"已解除"的问题：现在如实提示保存失败且不发手动解除日记事件；沉默轮与普通轮的状态保存失败也会记录诊断，不再静默丢失轮数递减。
- 修复调用②返回非字符串畸形响应时 `AttributeError` 穿透请求钩子、导致已有硬动作意外发出正式回复的问题：一律按调用失败走保守分支。
- 修复日记事件入队与 `diaryclear`/`reset` 清理之间的竞态：入队现在在 owner 提交锁内完成，清除前生成、清除后才落盘的事件不会再复活日记，也不会出现"磁盘有待处理事件、队列没有"的失联事件。
- 日记工具循环中途与提交落盘前都会复查来源会话的周期有效性（此前只查全局开关）：处理中周期失效即停，事件延后。
- 日记 worker 的会话周期门禁与请求链路同严：锚点/周期参数损坏导致周期不可计算时同样视为失效；Dashboard 永久删除会话现在与 `/period reset` 同款丢弃该会话的待处理日记事件（已提交日记保留）。
- 修复 `/period moodreset` 与 Dashboard 删除会话不持有情绪锁的问题：裸删除会被进行中请求链末尾的 `set` 复活旧状态；Dashboard 删除还会弹出锁对象导致同一会话出现两把锁并行。现在两者都在情绪锁内删除，且不再弹出锁；删除落盘失败时如实提示失败（指令回复重试、WebUI 返回错误）。
- 修复①②③只防非字符串响应、不防 `None` 响应对象的问题：三处统一安全提取响应文本，畸形 Provider 返回 `None` 或缺属性时落入保守分支，不再穿透请求钩子。
- 修复损坏的 v3 情绪文件（`params` 为字符串、`history` 为 null、记录不是对象等）反序列化抛异常打穿钩子的问题：`from_dict` 全字段防御，永不抛异常。
- 日记事件改为状态落盘成功后才下发：本轮的心境/动作事件先收集，`MoodStore.set` 失败时整批丢弃，日记不再记录实际没保存的变化；事件入队前复查来源会话周期有效性（请求中途被 reset/toggle 的事件不进 outbox），`submit` 失败也会记录诊断而非静默丢失。
- 修复损坏的 `moods.json`/`emotion_diaries.json` 仍可能在迁移/载入时抛异常、导致已有冷暴力/已读不回静默失效的问题：迁移逻辑对非标量容器免疫，日记文件逐本清洗（坏条目、缺路由字段的待处理事件一并规整或丢弃）。
- 修复自定义②提示词模板含未转义 `{` 时在模型调用前抛异常、绕过保守沉默的问题：模板构建失败按调用失败处理，已有硬动作照常沉默。
- 修复 Dashboard 删除会话的半成功与假成功：周期记录、情绪状态、待处理日记事件三项独立成败并返回组合明细，失败项可重试本接口继续清理（不再因周期已删而报"会话不存在"卡死）。
- 修复 `/period reset`（及 Dashboard 删除会话）与进行中请求之间的最后竞态：reset 现在记录持久化的丢弃水位线，晚到的旧周期事件入队即被存储层拒绝、存量事件由后台直接丢弃且不调用模型，插件重启后仍然生效。
- 日记注入按当前 `diary_max_chars` 实时截断：调低上限后、后台裁剪完成前，超限旧日记不再完整进入模型上下文。
- 持久化状态的布尔/计数字段严格校验（`"false"` 不再被当成真、 `true` 不再被当成轮数）；v3 历史记录只保留白名单键（外部数据不得带回用户原话）；JSON 解析失败日志不再包含响应首字符；畸形 Provider 对象属性访问异常也按调用失败处理。
- 修复 `global` 情绪模式下通过 Dashboard 删除一个不存在（或打错字）的会话会清空全局共享情绪的问题：全局情绪不再是"会话存在"的证据，也不再随单个会话删除而清除（由 `/period moodreset` 统一管理，面板成功提示中会说明）。
- 修复旧版 `moods.json` 中动作 `params` 为非法值时 `dict()` 抛异常、导致所有用户情绪状态无法加载的问题：非法 `params` 一律置空并记录诊断。
- 周期会话存储（CycleStore）改为事务语义：落盘成功才更新缓存、失败如实返回且不再污染内存；`set`/`toggle`/`advance`/`anchor`/`reset`/Dashboard 各写路径在保存失败时都会明确报错而不是假成功，`get()` 返回深拷贝避免调用方意外污染缓存。
- Dashboard 删除会话时日记清理（含丢弃水位线）最先执行：水位线写入失败会立即中止并保留周期记录，重试接口可从头完成；一旦发起删除，该会话的旧日记事件即失效且不因后续步骤失败而恢复。
- 修复损坏日记条目缺少 `id` 时后台工具循环抛 `KeyError`、永久阻塞该用户日记队列的问题：载入时补生成 `id` 而不是丢弃条目。
- v3 情绪历史中的非白名单字段（如外部写入的 `user_message`）现在不仅载入时剥离，还会触发落盘重写，磁盘上的原文一并清除；含非对象条目或整体不是列表的 `history` 同样覆盖。
- 修复 Dashboard 删除会话的情绪快照竞态：快照后进行中请求首次写入情绪时，`per_umo` 删除现在无条件获取情绪锁并在锁内重新检查，不再按旧快照谎报"已删除"。
- 修复旧编码 UMO 会话迁移的两处问题：两条记录并存时规范记录优先（别名只补齐缺失字段，不再把新锚点/开关/快进覆盖回旧值）；目标写入失败时返回原 UMO 且不搬迁运行态缓存（不再假 404/重复记录）。
- 周期会话存储补两个边界：`set()` 在存储边界深拷贝（调用方事后改原字典不再让缓存与磁盘不一致）；载入时忽略值不是对象的损坏记录（不再让请求链/Dashboard 抛 `TypeError`/`AttributeError`）。
- 修复 Dashboard 删除会话与在途请求的最后一条并发路径：情绪锁现在覆盖整个删除流程（`global` 模式同样取锁），在途请求锁前提交的日记事件随即被清理、锁后提交被源头复查挡掉，不再在 outbox 留下"水位线后提交"的残留事件。
- 请求链新增锁内二次门禁：已通过入口门禁但排队等待情绪锁的请求，在周期被 reset/删除/toggle 后拿到锁时直接结束本轮——不写过期的情绪/日记，外层也不再注入身体提示。
- 锁内门禁现在先检查情绪总开关：等锁期间管理员关闭情绪系统后，排队请求跳过三段调用与状态提交（身体周期提示照常注入）；已有硬动作与心境保留但立即停止拦截，`/period lift` 仍可解除。
- 修复周期删除写盘失败时仍清空预热/间隔运行态计数器的问题：现在只在删除成功后清理。
- v3 `history` 显式为 `null` 与字段缺失区分开：显式 `null` 同样触发迁移重写（此前内存清空但磁盘保留 `null`）。

### 兼容性

- 仍要求 AstrBot `>=4.24`；在 AstrBot 4.27.2 参考源码上完成核对。
- 旧 `moods.json` 自动幂等迁移到新结构：删除消息原文、软动作不再恢复、合法冷暴力/已读不回保留、双硬动作并存时清除并记录诊断。
- 第三方 Agent Runner 缺少正式会话/历史时跳过情绪与日记并记录限频诊断；一期完整支持内部 Agent 与 QQ 非流式链路。

## 2.2.0 - 2026-06-20

### 新增

- 新增 AstrBot 插件 Dashboard 完整配置面板，可在 WebUI 中查看、编辑并保存 `_conf_schema.json` 中的全部设置项。
- 新增 `GET /astrbot_plugin_period/config` 与 `POST /astrbot_plugin_period/config`，支持 schema、默认值、provider 下拉选项和运行态即时生效。
- WebUI 新增分组设置页：周期基础、注入与指令、提示词显示、生效范围、情绪系统、提示词压缩。
- WebUI 情绪系统分组增加实验性功能警告，并在开启情绪系统时进行二次确认。
- WebUI 下拉框、标签编辑器、数字输入框、会话列表和总览卡片完成视觉升级，统一为黑白留白卡片风格。

### 调整

- 默认动态提示词注入位置改为 `extra_user_content_parts`，对齐 AstrBot 临时内容注入方式，减少对用户原始消息和对话历史的污染。
- 动态提示词最大字数默认值改为 `0`，表示默认不截断；仍可在配置中设置正整数限制长度。
- 更新内置身体感知提示词与四阶段默认文案，使提示更强调保持原人格、避免主动暴露阶段名、减少过度表现。
- WebUI 保存配置时会保留未知旧字段和条件隐藏字段，避免误删未来配置或用户已有隐藏配置。

### 修复

- 修复 WebUI 保存后配置未立即同步到插件运行态的问题。
- 修复 provider 选择、嵌套对象、列表字段、长文本字段在 WebUI 保存时的校验和合并问题。
- 修复默认注入位置相关测试，使未显式配置时走 `extra_user_content_parts`。

### 兼容性

- 仍要求 AstrBot `>=4.24`。
- 已存在的用户配置不会被 WebUI 保存流程主动删除；新增默认值只会在字段缺失时作为兜底使用。
